// when I press play:
/*
1) AC.resume() is called
2) setIntervalTime(12) is called, which starts the startInterval loop
    1) 
*/

export const check = (msg) => {
    if (!msg) {
        console.log('here');
    } else {
        console.log(msg);
    }
    
    
}